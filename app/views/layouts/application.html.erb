<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Hotwire vs Inertia" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= vite_client_tag %>
    <%= vite_react_refresh_tag %>
    <%= vite_stylesheet_tag "application" %>
    <%= vite_javascript_tag "application" %>
    <%= vite_typescript_tag "inertia" %>
    <%= yield :head %>
  </head>
  <body class="text-white min-h-screen">
    <nav class="w-full glass-nav shadow-lg flex items-center justify-between px-6 h-14">
      <div class="flex items-center">
        <span class="font-bold text-base text-white tracking-tight">
          Hotwire vs Inertia
        </span>
      </div>
      <div class="flex space-x-3">
        <%= link_to "Hotwire", hotwire_feedbacks_path, class: "glass-link font-medium text-sm" %>
        <%= link_to "Inertia", inertia_feedbacks_path, class: "glass-link font-medium text-sm", data: { turbo: false } %>
      </div>
    </nav>

    <!-- Main Content Area -->
    <!-- Main Content Area -->
    <div class="flex h-[calc(100vh-3.5rem)]"> <!-- Adjusted for 56px navbar -->
      <!-- Sidebar -->
      <aside class="w-64 glass-sidebar flex flex-col py-8 px-4">
        <nav class="space-y-3">
          <%= link_to "▣ Dashboard", '#', class: "glass-link block text-sm py-3 px-4" %>
          <%= link_to "◐ Feedback", '#', class: "glass-link block text-sm py-3 px-4" %>
          <%= link_to "◈ Settings", '#', class: "glass-link block text-sm py-3 px-4" %>
          <%= link_to "◢ Analytics", '#', class: "glass-link block text-sm py-3 px-4" %>
          <%= link_to "◯ Notifications", '#', class: "glass-link block text-sm py-3 px-4" %>
        </nav>
      </aside>
      <!-- Main Content -->
      <main class="flex-1 p-8 overflow-y-auto bg-gradient-to-br from-transparent via-white/[0.02] to-transparent">
        <div class="max-w-6xl mx-auto">
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
